# Student Data Pipeline

This project cleans student CSV data, assigns grades, and writes cleaned CSV and a summary report.

How to run:

1. Ensure Python and `pandas` are installed.
2. From the project folder, run:

```powershell
python data_pipeline.py
```

To push to GitHub: initialize a git repo, create a remote, then `git push` (or use `gh repo create`).
